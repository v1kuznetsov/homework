{% extends 'base.html.twig' %}

{% block title %}{{ user.name }}{% endblock %}

{% block body %}
  <div>
  <h1>Привет {{ user.name }}</h3>
    <h3>Проверь свои данные</h3>
    <img src="../images/profile.svg.png" height="100" />
    <p>Почта: {{ user.email }}</p>
    <p>Телефон: {{ user.phone }}</p>
    <p>Город: {{ user.city }}</p>
    <p>Адресс: {{ user.adress }}</p>
  </div>
{% endblock %}